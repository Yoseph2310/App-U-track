import 'package:flutter/material.dart';

class AppColors {
  // Fondos
  static const Color background     = Color(0xFFF1EFE8);
  static const Color surface        = Color(0xFFFFFFFF);

  // Primario — teal
  static const Color primary        = Color(0xFF0F6E56);
  static const Color primaryLight   = Color(0xFFE1F5EE);
  static const Color primaryDark    = Color(0xFF085041);

  // Secundario — azul
  static const Color secondary      = Color(0xFF185FA5);
  static const Color secondaryLight = Color(0xFFE6F1FB);
  static const Color secondaryDark  = Color(0xFF0C447C);

  // Estados semánticos
  static const Color okBg           = Color(0xFFEAF3DE);
  static const Color okText         = Color(0xFF27500A);
  static const Color warnBg         = Color(0xFFFAEEDA);
  static const Color warnText       = Color(0xFF854F0B);
  static const Color errorBg        = Color(0xFFFCEBEB);
  static const Color errorText      = Color(0xFFA32D2D);

  // Texto
  static const Color textMain       = Color(0xFF2C2C2A);
  static const Color textSecondary  = Color(0xFF5F5E5A);

  // Bordes
  static const Color border         = Color(0xFFD3D1C7);
}